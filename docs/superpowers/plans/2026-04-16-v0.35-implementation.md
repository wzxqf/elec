# v0.35 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改变双层 HPSO 正式主线的前提下，实现 v0.35 的模块1正式化、超额收益验证链、政策风险调整后夏普、报告扩展，并保持 `run_all.py` 可完整跑通全项目。

**Architecture:** 先在周度数据层与物化层补齐 v0.35 所需基础字段，再新增独立分析层模块输出模块1表、风险因子表和超额收益验证表，最后把分析层挂到 `evaluate`、`backtest`、`run_pipeline`，确保所有正式产物落到 `outputs/<version>/...`。训练热路径仍保持 `HYBRID_PSO_V033` 主链，不把新增分析指标回灌到正式训练目标。

**Tech Stack:** Python, pandas, PyTorch, pytest, Markdown/YAML outputs

---

### Task 1: 补齐 v0.35 周度基础字段与物化输出

**Files:**
- Modify: `experiment_config.yaml`
- Modify: `src/data/weekly_builder.py`
- Modify: `src/backtest/materialize.py`
- Test: `tests/test_v035_weekly_builder.py`
- Test: `tests/test_v035_materialize.py`
- Test: `tests/test_run_all_script.py`

- [ ] **Step 1: 写周度基础字段的失败测试**

```python
from __future__ import annotations

import pandas as pd

from src.data.weekly_builder import build_weekly_bundle


def test_build_weekly_bundle_emits_v035_weekly_fields() -> None:
    frame = pd.DataFrame(
        {
            "datetime": pd.date_range("2025-11-03", periods=7 * 24 * 4, freq="15min"),
            "省调负荷_日前": 1000.0,
            "省调负荷_日内": 1005.0,
            "新能源负荷-总加_日前": 150.0,
            "新能源负荷-总加_日内": 152.0,
            "风电出力_日前": 80.0,
            "风电出力_日内": 81.0,
            "光伏出力_日前": 70.0,
            "光伏出力_日内": 71.0,
            "全网统一出清价格_日前": 320.0,
            "全网统一出清价格_日内": 330.0,
        }
    )
    config = {
        "feature_quantiles": [0.25, 0.5, 0.75],
        "lt_price": {"warmup_label": "warmup_unavailable"},
    }

    bundle = build_weekly_bundle(frame, config)
    weekly = bundle["weekly_metadata"]

    assert "da_id_cross_corr_w" in weekly.columns
    assert "extreme_event_flag_w" in weekly.columns
    assert "extreme_price_spike_flag_w" in weekly.columns
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_v035_weekly_builder.py -q`
Expected: FAIL because the new v0.35 columns do not exist yet.

- [ ] **Step 3: 写物化输出的失败测试**

```python
from __future__ import annotations

import pandas as pd

from src.backtest.materialize import materialize_particle_pair
from src.training.tensor_bundle import compile_training_tensor_bundle


def test_materialize_particle_pair_emits_v035_analysis_columns() -> None:
    weeks = pd.to_datetime(["2026-01-05"])
    bundle = {
        "weekly_features": pd.DataFrame({"week_start": weeks, "feature_a": [1.0], "feature_b": [0.5]}),
        "weekly_metadata": pd.DataFrame(
            {
                "week_start": weeks,
                "forecast_weekly_net_demand_mwh": [1000.0],
                "actual_weekly_net_demand_mwh": [980.0],
                "lt_price_w": [300.0],
                "da_price_mean": [320.0],
                "id_price_mean": [330.0],
                "spread_std": [5.0],
                "da_id_cross_corr_w": [0.8],
                "extreme_event_flag_w": [0.0],
                "extreme_price_spike_flag_w": [0.0],
            }
        ),
        "policy_state_trace": pd.DataFrame({"week_start": weeks, "renewable_mechanism_active": [1.0]}),
        "hourly": pd.DataFrame(
            {
                "week_start": [weeks[0], weeks[0]],
                "hour_index": [0, 1],
                "net_load_da": [100.0, 110.0],
                "net_load_id": [101.0, 112.0],
                "price_spread": [8.0, 9.0],
                "load_dev": [1.0, 2.0],
                "renewable_dev": [3.0, 4.0],
            }
        ),
        "quarter": pd.DataFrame(
            {
                "week_start": [weeks[0], weeks[0]],
                "interval_index": [0, 1],
                "全网统一出清价格_日前": [320.0, 321.0],
                "全网统一出清价格_日内": [330.0, 331.0],
            }
        ),
        "agent_feature_columns": ["feature_a", "feature_b"],
    }
    tensor_bundle = compile_training_tensor_bundle(bundle, device="cpu")

    result = materialize_particle_pair(
        tensor_bundle=tensor_bundle,
        upper_particle=[0.0] * 12,
        lower_particle=[0.0] * 8,
        strategy_name="validation",
    )

    assert "lock_ratio_proxy_w" in result.weekly_results.columns
    assert "curve_match_score_w" in result.weekly_results.columns
```

- [ ] **Step 4: 运行测试确认失败**

Run: `pytest tests/test_v035_materialize.py -q`
Expected: FAIL because the v0.35 weekly analysis columns are not emitted yet.

- [ ] **Step 5: 实现周度基础字段与物化字段**

```python
# experiment_config.yaml
project:
  version: v0.35

analysis_v035:
  price_spike_zscore_threshold: 2.5
  extreme_event_std_threshold: 2.0
  policy_risk_epsilon: 1.0e-6
```

```python
# src/data/weekly_builder.py
weekly_metadata["da_id_cross_corr_w"] = ...
weekly_metadata["extreme_price_spike_flag_w"] = ...
weekly_metadata["extreme_event_flag_w"] = ...
```

```python
# src/backtest/materialize.py
weekly_row["lock_ratio_proxy_w"] = float(contract_position[week_pos] / max(actual_load[week_pos], 1.0))
weekly_row["curve_match_score_w"] = ...
```

- [ ] **Step 6: 运行测试确认通过**

Run: `pytest tests/test_v035_weekly_builder.py tests/test_v035_materialize.py tests/test_run_all_script.py -q`
Expected: PASS and `run_all.py --dry-run` reports `Experiment version: v0.35`.

### Task 2: 实现模块1分析层

**Files:**
- Create: `src/analysis/module1.py`
- Test: `tests/test_v035_module1_analysis.py`

- [ ] **Step 1: 写模块1分析的失败测试**

```python
from __future__ import annotations

import pandas as pd

from src.analysis.module1 import build_contract_value_weekly, build_risk_factor_manifest


def test_build_contract_value_weekly_emits_required_columns() -> None:
    weekly_results = pd.DataFrame(
        {
            "week_start": pd.to_datetime(["2026-01-05"]),
            "contract_position_mwh": [600.0],
            "forecast_weekly_net_demand_mwh": [1000.0],
            "hedge_error_w": [0.05],
            "cvar99_w": [100.0],
            "contract_curve_h1": [0.04],
            "contract_curve_h2": [0.04],
            "da_price_mean": [320.0],
            "id_price_mean": [330.0],
            "spread_std": [5.0],
            "da_id_cross_corr_w": [0.8],
            "extreme_event_flag_w": [0.0],
            "extreme_price_spike_flag_w": [0.0],
        }
    )

    contract_value = build_contract_value_weekly(weekly_results)

    assert {"expected_spot_price_w", "liquidity_premium_w", "contract_value_w", "lock_ratio_proxy_w"} <= set(contract_value.columns)


def test_build_risk_factor_manifest_emits_five_categories() -> None:
    weekly_results = pd.DataFrame(
        {
            "week_start": pd.to_datetime(["2026-01-05"]),
            "da_price_std": [10.0],
            "id_price_std": [12.0],
            "spread_std": [5.0],
            "da_id_cross_corr_w": [0.8],
            "forecast_weekly_net_demand_mwh": [1000.0],
            "actual_weekly_net_demand_mwh": [980.0],
            "renewable_dev_std": [15.0],
            "prev_renewable_ratio_da_mean": [0.2],
            "extreme_event_flag_w": [1.0],
            "extreme_price_spike_flag_w": [1.0],
        }
    )

    manifest = build_risk_factor_manifest(weekly_results)

    assert manifest["factor_category"].nunique() == 5
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_v035_module1_analysis.py -q`
Expected: FAIL because `src.analysis.module1` does not exist yet.

- [ ] **Step 3: 实现模块1分析模块**

```python
def build_contract_value_weekly(weekly_results: pd.DataFrame) -> pd.DataFrame:
    expected_spot = 0.6 * weekly_results["da_price_mean"] + 0.4 * weekly_results["id_price_mean"]
    liquidity = 0.5 * weekly_results["spread_std"] + 2.0 * weekly_results["extreme_price_spike_flag_w"]
    ...
```

```python
def build_risk_factor_manifest(weekly_results: pd.DataFrame) -> pd.DataFrame:
    rows = [...]
    return pd.DataFrame(rows)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_v035_module1_analysis.py -q`
Expected: PASS with required v0.35 columns and five factor categories emitted.

### Task 3: 实现超额收益验证链与政策风险调整分析

**Files:**
- Create: `src/analysis/excess_return.py`
- Test: `tests/test_v035_excess_return_analysis.py`

- [ ] **Step 1: 写超额收益分析的失败测试**

```python
from __future__ import annotations

import pandas as pd

from src.analysis.excess_return import build_policy_risk_adjusted_metrics, summarize_rolling_excess_return


def test_build_policy_risk_adjusted_metrics_emits_required_columns() -> None:
    weekly = pd.DataFrame(
        {
            "week_start": pd.to_datetime(["2026-01-05", "2026-01-12"]),
            "excess_profit_w": [10.0, -2.0],
            "policy_projection_active": [1.0, 0.0],
            "policy_violation_penalty_w": [3.0, 1.0],
            "renewable_mechanism_active": [0.0, 1.0],
            "forward_price_linkage_days": [5.0, 2.0],
        }
    )

    result = build_policy_risk_adjusted_metrics(weekly, epsilon=1.0e-6)

    assert "policy_risk_penalty_w" in result.columns
    assert "policy_risk_adjusted_excess_return_w" in result.columns


def test_summarize_rolling_excess_return_emits_conclusions() -> None:
    policy_metrics = pd.DataFrame(
        {
            "window_name": ["window_01", "window_01", "window_02", "window_02"],
            "policy_risk_adjusted_excess_return_w": [2.0, 1.0, -1.0, 3.0],
            "excess_profit_w": [3.0, 2.0, -1.0, 4.0],
        }
    )

    summary = summarize_rolling_excess_return(policy_metrics, epsilon=1.0e-6)

    assert "window_policy_risk_adjusted_sharpe" in summary.columns
    assert "dynamic_lock_only_outperformed" in summary.columns
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_v035_excess_return_analysis.py -q`
Expected: FAIL because `src.analysis.excess_return` does not exist yet.

- [ ] **Step 3: 实现超额收益分析模块**

```python
def build_policy_risk_adjusted_metrics(weekly_results: pd.DataFrame, epsilon: float) -> pd.DataFrame:
    switch_pressure = weekly_results.filter(regex="_active$").diff().abs().sum(axis=1).fillna(0.0)
    countdown_pressure = ...
    penalty = switch_pressure + countdown_pressure + weekly_results["policy_projection_active"] + weekly_results["policy_violation_penalty_w"]
    ...
```

```python
def summarize_rolling_excess_return(policy_metrics: pd.DataFrame, epsilon: float) -> pd.DataFrame:
    ...
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_v035_excess_return_analysis.py -q`
Expected: PASS with policy-risk-adjusted weekly metrics and rolling-window conclusions.

### Task 4: 集成脚本入口、报告与总运行链

**Files:**
- Create: `src/analysis/reporting.py`
- Modify: `src/scripts/evaluate.py`
- Modify: `src/scripts/backtest.py`
- Modify: `src/scripts/run_pipeline.py`
- Modify: `tests/test_run_all_progress.py`
- Create: `tests/test_v035_script_outputs.py`

- [ ] **Step 1: 写脚本集成的失败测试**

```python
from __future__ import annotations

from pathlib import Path

from src.analysis.reporting import build_module1_summary


def test_build_module1_summary_mentions_v035_outputs() -> None:
    text = build_module1_summary(
        contract_value_path=Path("outputs/v0.35/metrics/contract_value_weekly.csv"),
        risk_factor_path=Path("outputs/v0.35/metrics/risk_factor_manifest.csv"),
    )

    assert "contract_value_weekly.csv" in text
    assert "risk_factor_manifest.csv" in text
```

```python
def test_run_pipeline_status_stub_still_completes_with_v035_outputs(monkeypatch) -> None:
    ...
    assert final_payload["stage"] == "完成"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_run_all_progress.py tests/test_v035_script_outputs.py -q`
Expected: FAIL because reporting helpers and v0.35 output integration are not implemented yet.

- [ ] **Step 3: 实现报告与入口集成**

```python
# src/analysis/reporting.py
def build_module1_summary(...): ...
def build_market_mechanism_analysis(...): ...
def build_excess_return_validation_summary(...): ...
```

```python
# src/scripts/evaluate.py
contract_value = build_contract_value_weekly(validation.weekly_results)
contract_value.to_csv(...)
```

```python
# src/scripts/backtest.py
rolling_excess = summarize_rolling_excess_return(...)
rolling_excess.to_csv(...)
```

```python
# src/scripts/run_pipeline.py
save_markdown(build_module1_summary(...), ...)
save_markdown(build_market_mechanism_analysis(...), ...)
```

- [ ] **Step 4: 运行集成测试**

Run: `pytest tests/test_run_all_progress.py tests/test_v035_script_outputs.py -q`
Expected: PASS with v0.35 reporting hooks integrated and final pipeline status unchanged.

- [ ] **Step 5: 运行回归测试**

Run: `pytest tests/test_v033_materialize.py tests/test_v033_score_kernel.py tests/test_v033_rolling_pipeline.py tests/test_run_all_script.py tests/test_run_all_progress.py tests/test_v035_weekly_builder.py tests/test_v035_materialize.py tests/test_v035_module1_analysis.py tests/test_v035_excess_return_analysis.py tests/test_v035_script_outputs.py -q`
Expected: PASS with no regression to existing v0.33 behavior and new v0.35 outputs covered.
