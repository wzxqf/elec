# v0.31 市场规则约束归集版本说明

`v0.31` 继承 `v0.3` 的“双层 HPSO + 15 分钟代理结算回测”主线，不改变 HPSO 的核心优化公式。本版重点是把项目周期内市场规则对交易、结算和制度状态的限制归结到程序中，并生成可查阅的约束文本。

## 版本边界

- 主算法仍为双层 HPSO：上层周度中长期合约调整量，下层小时级现货合约修正量。
- 中长期合约继续只作为结算依据，不作为物理调度计划。
- 现货结算继续按 96 个 15 分钟时段汇总成本、波动率、CVaR 和套保误差。
- 当前制度状态改为按周初 `week_start` 判定，避免后期政策在周内生效时前移覆盖周初决策。
- 前瞻制度状态继续保留，用于提示未来切换，但不得替代当前制度状态。
- 新增 `outputs/v0.31/reports/market_rule_constraints.md`，集中呈现市场规则约束、模型映射和校验结果。

## 市场规则约束

本版将以下规则作为项目主线约束：

- 中长期合约仅作为结算依据管理市场风险，现货交易全电量集中竞价。
- 中长期交易按每天 24 个时段组织，周度底仓必须能分解到小时口径。
- 日前/实时现货按 96 个 15 分钟时段组织，正式回测必须基于 15 分钟代理结算轨迹。
- 零售用户同一合同周期内只能向一家售电公司购电，同一结算周期只能生效一个零售套餐；零售套餐作为收益端边界，不作为模型自由动作。
- 现货试运行期间省内调峰辅助服务暂停，辅助服务收益不进入主策略收益函数。
- 新能源机制电价执行期自 2026-01-01 起进入当前制度状态，2025-11 至 2025-12 仅作为竞价准备或前瞻状态。
- 2026 年度中长期价格“40%固定价格 + 60%实时联动价格”机制自 2026-02-01 起进入当前结算代理，不能用于 2025-11 至 2026-01。
- 计量数据和异常处理必须可追踪，继续保留数据质量报告和 15 分钟偏差结算轨迹。

## 本版数学公式

本版未改变 `v0.3` 的 HPSO 优化公式，仅补充制度约束和报告输出。

### 1. HPSO 更新式

对粒子 \(i\) 在第 \(t\) 轮的位置 \(x_i^t\)、速度 \(v_i^t\)、个体最优 \(p_i^t\)、全局最优 \(g^t\)，标准 PSO 更新为：

\[
v_i^{t+1}=\omega v_i^t+c_1 r_1\odot(p_i^t-x_i^t)+c_2 r_2\odot(g^t-x_i^t)
\]

\[
\tilde{x}_i^{t+1}=x_i^t+v_i^{t+1}
\]

退火扰动为：

\[
T_t=T_0\rho^t,\qquad
x_i^{t+1,0}=\tilde{x}_i^{t+1}+\epsilon_i\odot s\cdot \lambda T_t\cdot \kappa_t
\]

其中 \(s\) 是初始化搜索尺度，\(\lambda\) 是扰动强度，\(\kappa_t\) 在停滞时取较大值。对设置为无硬限制的合约维度，\(x\) 不再做上下界裁剪；有限边界仅用于初始化尺度。

BP 精修对每个粒子执行：

\[
x_i^{t,k+1}=x_i^{t,k}-\eta\cdot \operatorname{clip}(\nabla_x J(x_i^{t,k}), G)
\]

若 \(J(x_i^{t,K}) < J(x_i^{t,0})\)，接受精修后位置；否则保留精修前位置。

### 2. 上层合约目标

上层变量为：

\[
a_w=(\delta h_w,b_w)
\]

其中 \(\delta h_w\) 是不设硬限的中长期合约调整量，\(b_w\) 是诊断性敞口带宽。最终锁定比例为：

\[
h_w=h_w^{base}+\delta h_w
\]

中长期合约量与现货基准量为：

\[
Q_w^{LT}=h_w\hat{L}_w,\qquad Q_w^{spot}=\hat{L}_w-Q_w^{LT}
\]

上层目标函数为：

\[
J_U=\sum_w(Q_w^{LT}P_w^{LT}+Q_w^{spot}P_w^{DA})
+\alpha_r\sum_w |Q_w^{spot}|(|S_w|+100R_w)
+\alpha_{\Delta h}\sum_w |h_w-h_{w-1}|\bar{L}
+\alpha_b\sum_w |h_w-h_w^{base}|\bar{L}
+\alpha_c\sum_w\left(b_w\hat{L}_w+|\delta h_w|\bar{L}+\gamma_h\delta h_w^2\bar{L}\right)
\]

其中 \(S_w\) 是价差风险代理，\(R_w\) 是新能源扰动代理，\(\bar{L}\) 是周负荷均值尺度，\(\gamma_h\) 对应 `hpso.objective_weights.contract_adjustment_quadratic`。

### 3. 下层现货合约目标

下层变量为逐小时现货修正量：

\[
\Delta q_t
\]

基础现货需求与执行现货量为：

\[
q_t^{base}=\hat{l}_t-q_t^{LT},\qquad q_t^{spot}=q_t^{base}+\Delta q_t
\]

目标跟踪量为：

\[
q_t^{target}=l_t^{ID}-q_t^{LT}
\]

下层目标函数为：

\[
J_L=\sum_t q_t^{spot}P_t^{DA}
+\beta_r\sum_t |\Delta q_t|\left(|S_t|+0.1|D_t^L|+0.05|D_t^R|\right)
+\beta_s\sum_t|\Delta q_t-\Delta q_{t-1}|
+\beta_e\sum_t|q_t^{spot}-q_t^{target}|
+\beta_c\sum_t\left(|\Delta q_t|+\gamma_q\Delta q_t^2\right)
\]

其中 \(\gamma_q\) 对应 `hpso.objective_weights.spot_delta_quadratic`。下层不再对 \(q_t^{spot}\) 执行非负裁剪，不再对 \(\Delta q_t\) 执行带宽裁剪或平滑裁剪；这些量仅进入诊断字段。

### 4. 15 分钟代理结算

小时合约量映射到 15 分钟后：

\[
E_{\tau}^{LT}=0.25q_t^{LT},\qquad E_{\tau}^{spot}=0.25q_t^{spot}
\]

\[
E_{\tau}^{sch}=E_{\tau}^{LT}+E_{\tau}^{spot}
\]

\[
E_{\tau}^{imb}=|E_{\tau}^{actual}-E_{\tau}^{sch}|
\]

15 分钟采购成本为：

\[
C_\tau=E_{\tau}^{LT}P_w^{LT}+E_{\tau}^{spot}P_\tau^{DA}+E_{\tau}^{imb}P_\tau^{ID}\mu
\]

其中 \(\mu\) 是偏差惩罚倍率。

## 验证要求

- `renewable_mechanism_active` 不得在 2026-01-01 前进入当前制度状态。
- `lt_price_linked_active` 不得在 2026-02-01 前进入当前制度状态。
- 2026-01-26 周可出现价格联动前瞻状态，但不能出现当前价格联动状态。
- `market_rule_constraints.md` 必须列出市场规则、模型映射、来源文件和校验结果。
- 全部正式产物必须写入 `outputs/v0.31/<真实输出>`。
