# 历史测试归档

本目录用于存放已退出当前主测试集的历史版本测试文件。

- `v0.33`、`v0.35`、`v0.36` 的版本化测试已从根目录 `tests/` 移出，避免继续参与当前 `v0.44` 主线的默认 `pytest` 收集。
- 这些文件保留原始文件名，便于按版本追溯历史实现和回归口径。
- `tests/` 中仍保留 `v0.40`、`v0.41` 相关测试，因为它们当前仍在覆盖可行域、周度特征和版本报告等主线契约。
